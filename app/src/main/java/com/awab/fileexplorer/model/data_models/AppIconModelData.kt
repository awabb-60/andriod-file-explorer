package com.awab.fileexplorer.model.data_models

/**
 * a data class that represent a app icon model, it hold the data to get the app icon
 */
data class AppIconModelData (val path:String, val handleable:Boolean)