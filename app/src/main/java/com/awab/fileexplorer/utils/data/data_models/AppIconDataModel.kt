package com.awab.fileexplorer.utils.data.data_models

/**
 * a data class that represent a app icon model, it hold the data to get the app icon
 */
data class AppIconDataModel (val path:String, val handleable:Boolean)